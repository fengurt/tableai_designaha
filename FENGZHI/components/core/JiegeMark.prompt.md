界格徽记 — 峰值永造局的品牌符号，用于封面、页眉与落款；最小 12px，四周净空 ≥ 50% 宽度。

```jsx
<JiegeMark size={32} />
<JiegeMark size={20} reversed />  {/* 墨黑底 */}
<JiegeMark size={20} mono />      {/* 单色印刷 */}
```

不旋转、不加圆角、不改变金格位置（右上）。
