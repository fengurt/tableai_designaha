/** 眉题小字 — 0.22em 大字距，石灰色（或深金）。 */
export interface KickerProps {
  children?: any;
  /** 用深金强调（如三短语） */
  gold?: boolean;
  /** 字号 px，默认 11 */
  size?: number;
}
export declare function Kicker(props: KickerProps): JSX.Element;
